# W3 — med targeted re-pass (2026-09-10)

Selection: of 556 med rows, 245 whose evidence carries a figure cue
(Fig./figure/graph/shown in/plotted). Re-read against the CURRENT registry
(free_body_forces@1, xy_plot v4, lines_planes_3d modes, ten named shapes).

## Routed: 17 rows moved to high

projectile_motion 1 (Projectile Motion — the directive's expected gain),
process_flow 6 (Zeroth Law, Born-Haber ring, Sequences, Differentiation
cycle, Curd strip, PCR cycle), xy_plot 5 (Decay, Arrhenius, Lanthanoid data,
Calorimetry + Change of State via named `heating`), reaction_scheme 1
(Diazonium), lines_planes_3d 4 (Angle Between Lines, Plane Forms, Point-Plane
Distance, Foot/Image).

## The blind check

An independent agent, shown evidence only — 36 rows: the 18 proposed plus 18
of the refusals as controls, shuffled. All 18 controls confirmed "none".
Band agreement 17/18; the one miss was OURS: Raoult's Fig 1.3 draws THREE
lines (partials + total), xy_plot draws two. Removed. Per-subject after the
removal: every subject 100% on its surviving band (chem12's first pass was
3/4 = 75% — under the bar until corrected, and the correction was a removal,
never an addition the checker rejected).

## molecule_3d's expected gain did not materialise, and why

Its candidate (DNA double helix, real 3-D geometry, a PDB structure) carries
NO figure cue in its evidence text, so the subset rule itself excluded it.
The row is real and probably promotable — but not in THIS pass without
breaking the pass's own selection protocol. Left for a targeted look.

## Notable refusals (each with its defeating element)

Photoelectric ("no graph is shown in the chunks" — the cue word matched a
negation); ethane conformations (cosine at a maximum; sine kind has no
phase); Kohlrausch (root-c axis); Blackman (light-saturation is not
growth_curve — a name-match, not a figure-match); Raoult (three lines);
azeotropes (bowed vs dashed comparison curves); redox couples (two equally
load-bearing figures, one an apparatus); ABO grid (no trend axis); oxide
progression (rendered as bars, not a grid); Composition of Functions (two
flows in one figure); C4/Kranz (labelled two-compartment cell); the four
ch10 conic-tangent figures (not functions of x); math11 ch11's three
(2-D geometry / edge-view projection / tetrahedron centroid).

Routes: 97 -> 114. Chapters and high-count pins re-measured, not derived.
