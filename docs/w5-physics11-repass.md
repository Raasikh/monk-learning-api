# W5 — physics 11 re-pass, plate-first (2026-09-10)

Scope: the 58 physics-11 rows that were gap_*, low, or apparatus-med. The med
none_symbolic rows were already swept by W3's figure-cue pass and are out of
scope by prior coverage. Rule applied FIRST: is the load-bearing figure a
static labelled plate an illustrator draws once? Only then: which widget
computes it outright?

## Result

- 44 -> labelled_figure (the physics art backlog; corpus labelled_figure now
  137 rows, vs biology's 30 which already have art). Includes every
  thermodynamics schematic (piston, engine boxes, Carnot loop, the four-curve
  P-V plate), the ch9 fluid plates, the ch3 vector constructions, Cavendish
  and Searle apparatus, and the qualitative curve plates (Maxwell, entropy)
  whose shapes no curve kind computes.
- 6 routed: free_body_forces (banked road; rolling motion) and xy_plot
  (potential energy and shm_energy via NAMED shapes; stress-strain via
  named; gravitational U as reciprocal with the evidence's own axes).
- 1 -> data_table_trend at med (degrees of freedom: f by molecular
  structure — a trend table; med because the evidence is a dossier caption).
- 7 stay none_symbolic (no printed figure; one Pro-Tip literally says
  'no diagrams needed'); Young's modulus joins them (its figure belongs to
  Searle's experiment row).

## Blind check

Independent agent, evidence only, all 58 rows. First-pass agreement 52/58.
Its six differences, resolved by re-derivation rather than by authority:
ADOPTED — banked road and rolling ARE free_body_forces (the plate rule
yields to a widget that computes the figure outright, which is the rule's
own intent); SHM energy IS the named shm_energy shape; degrees-of-freedom IS
a trend table; field-vs-radius is a printable comparison plate; Young's has
no figure of its own. HELD — Mach cone: retrieval FAILED, and 'gap' would
assert a drawn figure nobody saw; it stays none_symbolic.

Routes delta: 114 -> 120 (labelled_figure is not a registered widget; its 44
are an art work-order, not routes). Pins re-measured: high 501, chapters 60.
