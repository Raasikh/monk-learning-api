fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
supabase>=2.3.0
PyJWT>=2.8.0
cryptography>=42.0.0
python-dotenv>=1.0.0
requests>=2.31.0
openai>=1.14.0
websockets>=12.0
# Required by FastAPI's UploadFile/File — Snap a Doubt posts multipart photos.
python-multipart>=0.0.9
# Cloudflare R2 is S3-compatible; boto3 stores and presigns snapped doubt images.
boto3>=1.34.0
# Snap a Doubt trims browser furniture off a screenshot before OCR — see
# app/image_prep.py. Pillow only: the row/column profiling that would normally
# want numpy is done by resizing the mask to a 1px strip.
Pillow>=10.0.0
